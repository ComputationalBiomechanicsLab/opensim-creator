#pragma once

// char[]
//
// name of the organization (affects configuration location)
#define OSC_ORGNAME_STRING "@OSC_ORGNAME@"

// char[]
//
// short-form name of the application (affects configuration location)
#define OSC_APPNAME_STRING "@OSC_APPNAME@"

// char[]
//
// user-friendly full application name string
#define OSC_LONG_APPNAME_STRING "@OSC_LONG_APPNAME@"

// char[]
//
// full version string of OSC (e.g. 1.0.3)
#define OSC_VERSION_STRING "@osc_VERSION_MAJOR@.@osc_VERSION_MINOR@.@osc_VERSION_PATCH@"

// char[]
//
// unique build ID for this build of OSC. Bakes the build ID into
// the binary, which is handy for user bug reports, etc.
#define OSC_BUILD_ID "@OSC_BUILD_ID@"

// char[]
//
// a URL for OSC's source code repo. Used by in-app links, shortcuts, etc. so that
// users can just click and go, rather than having to separately browse to it.
#define OSC_REPO_URL "@OSC_REPO_URL@"

// char[]
//
// a URL for the OSC's documentation Used by in-app links, shortcuts, etc. so that
// users can just click and go, rather than having to separately browse to it.
#define OSC_DOCS_URL "@OSC_DOCS_URL@"

// char[]
//
// the internet location for a "help site", which is loosely defined as somewhere the user
// can go to ask for help. This data is used by in-app links, shortcuts, etc.
#define OSC_HELP_URL "@OSC_HELP_URL@"

// define
//
// set if the application should also bundle the demo tabs
#cmakedefine OSC_BUNDLE_OSCAR_DEMOS
