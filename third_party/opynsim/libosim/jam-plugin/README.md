# jam-plugin

> Fork of https://github.com/opensim-jam-org/jam-plugin (e6300b8)

Fork changes:

- `CMakeLists.txt` was merged into the root one for `OpenSimThirdPartyPlugins`.
- `RegisterTypes_osimPlugin` was merged with the root one for `OpenSimThirdPartyPlugins`.
- Header includes were adjusted to match how the top-level project is structured.
