#pragma once

// define/enable/disable to toggle forcing `OSC_ASSERT*` macros to always
// run, regardless of the build's debug flags.
#cmakedefine01 OSCAR_FORCE_ASSERTS_ENABLED

// define/enable/disable to toggle whether the `OSC_PERF` macro actually
// records the runtime performance of the application
#cmakedefine01 OSCAR_RUNTIME_PERF_MEASUREMENTS_ENABLED
