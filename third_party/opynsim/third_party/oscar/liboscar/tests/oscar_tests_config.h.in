#pragma once

// char[]
//
// name of the organization (affects things like configuration location)
#define OSCAR_TESTS_ORGNAME_STRING "oscar"

// char[]
//
// full application name string
#define OSCAR_TESTS_APPNAME_STRING "oscar_tests"

// char[]
//
// absolute path to the build_resources/ dir (for access to test fixture data)
#define OSCAR_TESTS_RESOURCES_DIR "@CMAKE_CURRENT_SOURCE_DIR@/resources"
