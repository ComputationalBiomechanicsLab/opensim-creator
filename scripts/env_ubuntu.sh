# Sets up an Ubuntu terminal with the necessary environment to run
# OpenSim Creator correctly.

# A session DBUS must be launched in order for the file dialogs to work.
eval $(dbus-launch --sh-syntax)
