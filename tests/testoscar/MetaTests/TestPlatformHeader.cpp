#include <oscar/Platform.hpp>

#include <gtest/gtest.h>

TEST(PlatformHeader, CanBeCompiled)
{
	// quite meta, but this is just a way to ensure that `#include <oscar/Platform.hpp>`
	// compiles, and is handy for benchmarking how quickly it compiles
}
