#include <oscar/UI.h>

#include <gtest/gtest.h>

TEST(UIHeader, compiles)
{
    // quite meta, but this is just a way to ensure that `#include <oscar/UI.h>`
    // compiles, and is handy for benchmarking how quickly it compiles
}
