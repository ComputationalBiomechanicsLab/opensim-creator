#include "Platform.h"

#include <gtest/gtest.h>

TEST(PlatformHeader, compiles)
{
    // quite meta, but this is just a way to ensure that `#include <liboscar/Platform.h>`
    // compiles, and is handy for benchmarking how quickly it compiles
}
