#include "Maths.h"

#include <gtest/gtest.h>

TEST(MathsHeader, compiles)
{
    // quite meta, but this is just a way to ensure that `#include <liboscar/Maths.h>`
    // compiles, and is handy for benchmarking how quickly it compiles
}
