#include "Variant.h"

#include <gtest/gtest.h>

TEST(VariantHeader, compiles)
{
    // quite meta, but this is just a way to ensure that `#include <liboscar/Variant.h>`
    // compiles, and is handy for benchmarking how quickly it compiles
}
