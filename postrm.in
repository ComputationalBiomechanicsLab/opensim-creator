#!/usr/bin/env bash

# postrm: ran by the Debian package manager (dpkg) when osmv is
# uninstalled

# remove the symlink that was created at installation-time
rm -f "/usr/local/bin/osmv"

# remove the desktop icon
rm -f "/usr/share/applications/osmv.desktop"
